// Notification events — extended in Phase 6 with push notification service
// Currently, client-side notifications are triggered by the receive_message event

function registerNotificationEvents(_io, _socket) {}

module.exports = registerNotificationEvents
